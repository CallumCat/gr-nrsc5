# auto-generated by grc.converter

id: nrsc5_sis_encoder
label: SIS encoder
category: '[NRSC-5]'

parameters:
-   id: short_name
    label: Station name
    dtype: string
    default: ABCD
-   id: station_message
    label: Station message
    dtype: string
    default: ABCD

outputs:
-   domain: stream
    dtype: byte
    vlen: 80
asserts:
- ${ len(short_name) <= 16 }

templates:
    imports: import nrsc5
    make: nrsc5.sis_encoder(${short_name}, ${station_message},)

file_format: 1
